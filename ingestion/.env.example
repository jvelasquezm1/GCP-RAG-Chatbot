# Gemini API Configuration
# Required: Get your API key from https://makersuite.google.com/app/apikey
GEMINI_API_KEY=your-gemini-api-key

# GCP / Firestore Configuration
# Required: Your Google Cloud Platform project ID
GCP_PROJECT_ID=your-gcp-project-id

# Optional: Firestore collection name (defaults to "documents")
FIRESTORE_COLLECTION=documents

# Optional: Embedding model (defaults to "models/text-embedding-004")
GEMINI_EMBEDDING_MODEL=models/text-embedding-004

# Optional: Text chunking configuration (defaults shown)
CHUNK_SIZE=1000
CHUNK_OVERLAP=200

# Note: This script uses the same configuration as the backend.
# You can share the same .env file between backend/ and ingestion/ directories
# by placing it in the project root, or create separate .env files for each.
